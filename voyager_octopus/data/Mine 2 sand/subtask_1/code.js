async function mineSand(bot) {
  // Subtask 1: Teleport near the sand block in pic1 or pic2
  await teleport(0.00, 17.22); // Teleport to the sand block at distance 17.22 in pic1
}

await mineSand(bot);